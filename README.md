```
$ git clone {このリポジトリ}
$ cd hack_lashikia0123
$ npm ci
$ npm run start
```

```
$ git checkout {ブランチ}
$ git add .
$ git commit -m " {コメント} "
$ git push -u origin {ブランチ}
```

```
$ git checkout develop
$ git merge {ブランチ}
```
